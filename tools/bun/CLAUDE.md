## Bun
- `bun install --frozen-lockfile`. `bun run build` and `bun test 2>&1 | tail -n 40` before claiming done.
- No new dependency without saying why. Never `npm`/`yarn`/`pnpm`: the lockfile is Bun's.
